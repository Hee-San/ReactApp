{
  "name": "my-app",
  "version": "0.1.0",
  "homepage": "https://hee-san.github.io/ReactApp",
  "private": true,
  "dependencies": {
    "ajv": "^6.12.3",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-p5-wrapper": "0.0.4",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
